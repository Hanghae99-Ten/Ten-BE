package com.sparta.ten.accounts.repository;

import com.sparta.ten.accounts.domain.AccountGroupConnect;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccountGroupConnectRepository extends JpaRepository<AccountGroupConnect, Long> {
}
